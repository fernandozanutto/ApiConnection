<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ApiConnection" parent="Theme.MaterialComponents.DayNight">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/green</item> <!-- buttons background -->
        <item name="colorPrimaryDark">@color/light_gray</item> <!-- App top bar (notifications) -->
        <item name="colorOnPrimary">@color/dark_gray</item>
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">@color/gray</item>
        <item name="colorControlNormal">@color/dark_gray</item> <!-- Toolbar navigationIconTint -->

        <item name="android:windowLightStatusBar" tools:targetApi="23">true</item>

        <item name="fontFamily">sans-serif-light</item>
        <item name="android:textColor">@color/dark_gray</item>
    </style>

    <style name="Theme.ApiConnection.DefaultScreen">
        <item name="android:background">@color/light_gray</item>
    </style>

    <style name="Theme.ApiConnection.DefaultAppToolBar">
        <item name="android:background">@color/light_gray</item>
    </style>

    <style name="Theme.ApiConnection.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

</resources>